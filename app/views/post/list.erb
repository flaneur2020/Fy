<%= render :partial=>'share/head'%>

    <div id="main">
      <h1>文章列表</h1>
      <% if flash[:notice] %>
        <span class="alert"><%=flash[:notice]%></span>
      <% end -%>
      <span class="alert error">orz~ </span>

      <div id="list-main">
        <span class="list-nav">
          <span class="filter">
            <select> <option value="default">全部目录</option> </select>
            <select> <option value="default">全部日期</option> </select>
            <button>过滤</button>
          </span>
          <span class="pagi"> 前一页 | 1 2 3 4 5 | 后一页 </span>
        </span>
        <table>
          <thead>
            <tr> 
              <th id="select"> 
                <input type="checkbox"></input>
              </th>
              <th id="title">标题</th>    
              <th id="category">目录</th>    
              <th id="author">作者</th>    
              <th id="date">日期</th>    
            </tr>
          </thead>
          <tbody>
            <% @posts.each do |post| %>
              <tr valign="top">
                <th>
                  <input type="checkbox"></input>
                </th>
                <th>
                  <%=link_to post.title, :action=>:view, :id=>post.id%>
                  <span class="act">
                    <%=link_to '编辑', :action=>:edit, :id=>post.id %> 
                    <%=link_to '删除', {:action=>:rm, :id=>post.id}, {:confirm=>"真的要删除文章'"+post.title+">嘛？", :style=>'color:red'} %>
                    <%=link_to '查看', :action=>:view, :id=>post.id %> 
                  </span>
                </th>
                <th><a href="#">默认</a></th>
                <th><%=link_to post.user.name, :controller=>:user, :action=>:view, :id=>post.user.id%></th>
                <th><%=post.updated_at%></th>
              </tr>
            <% end %>
          </tbody>
          <tfoot>
            <tr> 
              <th id="select">
                <input type="checkbox"></input>
              </th>
              <th id="title">标题</th>    
              <th id="category">目录</th>    
              <th id="author">作者</th>    
              <th id="date">日期</th>    
            </tr>
          </tfoot>
        </table>

        <span class="list-nav">
          <span class="filter">
            <select> <option value="default">批量操作</option> </select>
            <button>应用</button>
          </span>
          <span class="pagi"> 前一页 | 1 2 3 4 5 | 后一页 </span>
        </span>
      </div>
    </div>

<%= render :partial=>'share/foot'%>
